rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Horarios: permitir lectura (la app no usa Firebase Auth; en producción restringir con App Check o auth)
    match /horarios/{courseId} {
      allow read: if true;
      allow write: if false;
    }
  }
}
